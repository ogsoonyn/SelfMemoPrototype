<Window x:Class="SelfMemoPrototype.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SelfMemoPrototype"
        xmlns:vm="clr-namespace:SelfMemoPrototype.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="5" HorizontalAlignment="Center">
            <TextBlock Text="用語: "/>
            <TextBox Width="80" Text="{Binding Word.Value}"/>

            <TextBlock Text="略語・類義語等: "/>
            <TextBox Width="80" Text="{Binding ShortWord.Value}"/>
            
            <TextBlock Text="説明: "/>
            <TextBox Width="80" Text="{Binding Description.Value}"/>

            <TextBlock Text="カテゴリ: "/>
            <TextBox Width="80" Text="{Binding Category.Value}"/>

            <Button Width="50" DockPanel.Dock="Bottom" Content="Add" Command="{Binding Cmd}"/>
            <Button Width="50" DockPanel.Dock="Bottom" Content="Select" Command="{Binding Cmd2}"/>
            <Button Width="50" DockPanel.Dock="Bottom" Content="Reset" Command="{Binding Cmd3}"/>

            <CheckBox Content="ReadOnly" IsChecked="{Binding GridReadOnly.Value}"/>
        </StackPanel>
        <DataGrid DockPanel.Dock="Top" ItemsSource="{Binding MemoList}" IsReadOnly="{Binding GridReadOnly.Value}"/>
    </DockPanel>
</Window>
